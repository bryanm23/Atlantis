# Atlantis Talent Group — Homepage

Static, dependency-free homepage for **Atlantis Talent Group**.

## Run locally

- Open `index.html` directly in your browser, or
- Use a tiny local server (recommended):

```bash
cd "/Volumes/T7/Cursor:Code/Atlantis"
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.

## Customize

- Copy updates: `index.html`
- Colors / layout: `styles.css`
- Mobile nav + demo form behavior: `script.js`

> The contact form is currently demo-only (no backend). Wire it to your CRM/email provider when ready.
